# Design System: Warm Storytelling (Airbnb-inspired)

このドキュメントは、AIにLPを生成させる際のデザインルールを定義したものです。
「AI生成っぽさ」を排除し、人の手で作られたような温かみのある仕上がりを目指します。

---

## 1. デザイン哲学

- **Storytelling over Selling**: 売り込まず、物語る
- **Editorial over Marketing**: 雑誌のような落ち着き
- **Quiet Luxury**: 派手にせず、質感で価値を伝える
- **Human Imperfection**: 完璧すぎる整列を避け、余白で呼吸させる

---

## 2. カラーパレット

### ベースカラー
- `#FAF7F2` (warm off-white / 和紙の白) — 背景
- `#1A1A1A` (charcoal) — メイン文字色 (純黒は使わない)
- `#5C5C5C` (warm gray) — サブ文字色

### アクセントカラー
- `#8B6F47` (落ち着いた茶色 / 木の色) — メインアクセント
- `#C9B89A` (薄いベージュ) — 補助
- `#6B7A4B` (深い苔色) — 自然要素のアクセント

### 禁止
- 純黒 (#000000)、純白 (#FFFFFF) は使わない
- 彩度の高い原色は禁止
- グラデーションは極力避ける (使うなら同系色のごく淡いもの)

---

## 3. タイポグラフィ

### フォント選定
- **和文見出し**: `"Noto Serif JP", serif` (weight: 500-700)
- **和文本文**: `"Noto Sans JP", sans-serif` (weight: 400)
- **欧文見出し**: `"Cormorant Garamond", "Playfair Display", serif`
- **欧文サブ**: `"Inter", sans-serif` (weight: 400-500)

### サイズスケール (rem基準)
- Hero見出し: 4rem〜6rem (clamp使用)
- Section見出し: 2.5rem〜3.5rem
- 本文: 1rem〜1.125rem
- キャプション: 0.875rem

### ルール
- 見出しは line-height 1.2〜1.3
- 本文は line-height 1.7〜1.8 (日本語は広めに)
- letter-spacing は 和文 0.05em、欧文 0.02em
- 見出しに `font-weight: 700` 以上は使わない (重すぎる)

---

## 4. レイアウト原則

### 余白
- セクション間の上下余白: **120px〜180px** (PCの場合)
- セクション内の要素間: 32px〜64px
- カードの内側パディング: 32px〜48px

### グリッド
- 最大幅 1200px (1280pxまで)
- カラム間ガター: 32px
- 12カラムグリッドだが、コンテンツは8〜10カラムに収める

### 重要原則
- **片寄せレイアウトを積極的に使う** (中央寄せばかりにしない)
- 写真とテキストの非対称配置で「雑誌っぽさ」を出す
- 1セクション1メッセージ (情報を詰め込まない)

---

## 5. ビジュアル要素

### 写真の扱い
- アスペクト比は固定しすぎない (4:5、3:4、16:9を混在)
- 角丸 16px〜24px (写真も)
- ホバー時の軽い拡大 (scale: 1.03、duration 600ms以上)
- 写真にうっすらノイズテクスチャを乗せると「印刷物っぽさ」が出る

### ボタン
- 角丸 999px (ピル型) または 8px (シャープ型) ※統一する
- パディング: 縦 16px / 横 32px 以上
- ホバー時は色反転 or 微細な背景変化のみ
- **影は使わない** (フラットに保つ)

### カード
- 背景は `#FFFFFF` ではなく `#FAF7F2` の僅かな調整
- ボーダーは `#E8E2D8` の極細線
- 影は `0 1px 3px rgba(0,0,0,0.04)` 程度の極弱

### アイコン
- 線画 (stroke) のみ使用、塗りつぶしは禁止
- ストローク幅 1.5px
- Lucide / Phosphor Light を推奨

---

## 6. インタラクション

- アニメーションは **存在を主張しない**
- スクロール時のフェードイン: opacity 0→1、translateY 20px→0、duration 800ms、ease-out
- ホバーアニメーションは 300ms以下
- パララックスは使わない (野暮ったい)

---

## 7. 避けるべきAIっぽい表現

❌ **使ってはいけないもの**
- グラデーションの背景 (特に紫→ピンク系)
- ネオンっぽい光彩効果
- 等間隔・完璧に整列したカード
- 同じサイズの写真をグリッドで並べるだけの構成
- 「Lorem Ipsum」っぽい意味のない英語コピー
- アイコン+ラベルの3カラム並びの「特徴セクション」
- ヒーローに大きな丸ボタン2つ並べ

✅ **代わりに使うべきもの**
- 余白で空気を作る
- 写真サイズを意図的にバラつかせる
- セリフ体で「人の手」を感じさせる
- 数字や引用で物語を語る
- 短い詩的なコピー

---

## 8. コピーライティングのトーン

- ですます調を基本とする
- 一文を短く (40字以内が目安)
- 修飾語を削ぎ落とす
- 読み手に語りかけるよりも、世界観を見せる
- 例: ❌「最高のひとときをお届けします!」
       ✅「庭の風が、お茶の香りを運びます。」

---

## 9. レスポンシブ設計

- モバイルファースト
- ブレイクポイント: 640px / 768px / 1024px / 1280px
- モバイルでは余白を 60〜80% に縮める
- 写真は縦長(4:5)に切り替えるのも可
